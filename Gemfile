source 'https://rubygems.org'
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in simple_form-themes.gemspec.
gemspec

group :development do
  gem 'sqlite3'
end

group :test do
  gem 'byebug', group: %i[development test]
end
