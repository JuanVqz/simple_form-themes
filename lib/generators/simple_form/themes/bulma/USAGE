Description:
    Install the simple form bulma config file in your app.

Example:
    bin/rails generate simple_form:themes:bulma

    This will create:
        config/initializers/simple_form_bulma.rb
